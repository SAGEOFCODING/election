version: '3.8'
services:
  web:
    build: .
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
      - NODE_ENV=production
    # If testing locally, mount volumes for live reload:
    # volumes:
    #   - .:/usr/src/app
    #   - /usr/src/app/node_modules
